<html>
  <%= render "nav_header" %>

  <body>
    <div class="container one-image">
      <%= image_tag(@image.image_url) %>
      <% @image.tag_list.each do |tag| %>
          <%= tag %>
      <% end %>
    </div>
  </body>

  <%= render "footer" %>
</html>
